# Future Sessions Plan

## Current State

The fork marketplace publishes `session-handoff` version `1.9.2` with two nested skills: `handoff` and `handload`.

## Priority Actions

1. Verify the reloaded Claude Code process exposes `/session-handoff:handoff` and `/session-handoff:handload`.
2. If the plugin changes again, preserve the `plugins/session-handoff/skills/<skill>/SKILL.md` layout and increment every published version field together.

## Decision Queue

- No open architecture decisions.
